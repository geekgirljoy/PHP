<?php
/**
 * Does Something
 *
 * @param $hs Add Description
 * @param $fcx Add Description
 *
 */
function Qn($hs, $fcx){ 
    return sinh($hs) - bindec($fcx);
}


/**
 * Does Something
 *
 * @param $ex Add Description
 * @param $isihd Add Description
 *
 */
function Pgspj($ex, $isihd){ 
    return ceil($ex) - decoct($isihd);
}


/**
 * Does Something
 *
 * @param $xixy Add Description
 * @param $urb Add Description
 *
 */
function Pozztiw($xixy, $urb){ 
    return tanh($xixy) % sqrt($urb);
}


/**
 * Does Something
 *
 * @param $qit Add Description
 * @param $zkpem Add Description
 *
 */
function Cbehgy($qit, $zkpem){ 
    return max($qit) - octdec($zkpem);
}


/**
 * Does Something
 *
 * @param $hkogp Add Description
 * @param $yvicm Add Description
 *
 */
function Khduwxr($hkogp, $yvicm){ 
    return abs($hkogp) * hexdec($yvicm);
}


